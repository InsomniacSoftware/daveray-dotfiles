#!/bin/sh

# eureka test service
# eureka prod service
curl -s -i \
    http://us-east-1.discovery$([ "$1" == "test" ] && echo "test" || echo "prod").netflix.net:7001/discovery/resolver/${2}/ \
  | perl -n -e '/^Location: (.+)\// && print $1'
